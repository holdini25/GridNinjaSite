// Shared identifiers carry no fixture data or runtime validation dependency.
export const DEFAULT_ASSESSMENT_SCENARIO = "b" as const
export const ASSESSMENT_PUBLICATION_VERSION = "1.0.0"
export const ASSESSMENT_SCENARIOS = ["a", "b", "c", "d"] as const
export const ASSESSMENT_PERSPECTIVES = ["business", "engineering"] as const
