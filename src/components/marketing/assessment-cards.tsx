export function AssessmentCards({ items, variant = "cards" }: { items: readonly { title: string; body: string }[]; variant?: "cards" | "rows" }) {
  if (variant === "rows") return <div className="divide-y divide-divider border-y border-divider">{items.map(item => <article key={item.title} className="grid gap-3 py-6 md:grid-cols-[minmax(0,.7fr)_minmax(0,1.3fr)] md:gap-10"><h3 className="text-xl font-medium">{item.title}</h3><p className="max-w-prose text-base leading-7 text-muted-foreground">{item.body}</p></article>)}</div>
  return <div className="grid gap-4 md:grid-cols-2">{items.map(item => <article key={item.title} className="rounded-xl border border-border/80 bg-surface p-6 sm:p-7"><h3 className="text-xl font-medium">{item.title}</h3><p className="mt-3 max-w-prose text-base leading-7 text-muted-foreground">{item.body}</p></article>)}</div>
}
